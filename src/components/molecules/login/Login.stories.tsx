import { Login } from "./Login";

export default {
  title: "Login",
  component: Login,
};

export const light = {
  args: {
    variant: "light",
  },
};

export const dark = {
  args: {
    variant: "dark",
  },
};